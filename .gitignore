__pycache__/
*.py[cod]
*.egg-info/
.coverage
.pytest_cache/
.ruff_cache/
.mypy_cache/
.venv/
dist/
build/

# Runtime state is never source code.
.env
dataset/
artifacts/
outputs/
*.db
*.db-shm
*.db-wal

# Local model and experiment output.
checkpoints/
runs/
wandb/

# Local Claude Code configuration can contain provider credentials. Keep the
# reusable plugin source, but never commit machine-specific settings.
.claude/settings.json
.claude/settings.local.json

